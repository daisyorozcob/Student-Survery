document.querySelector("form").onsubmit = function(event) {
    event.preventDefault();
    hideResults();
    const  = parseInt(document.querySelector("").value);
    const  = parseInt(document.querySelector("").value);

    // we're evaluating the Tower of Doom ride first
    if (>=  && >= ) {
      document.getElementById("").removeAttribute("");
      document.getElementById("").removeAttribute("");
      document.getElementById("").removeAttribute("");
    } 
  };